<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="/css/home.css">
    <title>Details</title>
</head>
<body>
    <h1>Expense Details</h1>
    <p><strong>Content:</strong> <%= expense.content %></p>
    <p><strong>Email:</strong> <%= expense.email %></p>
    <p><strong>Created At:</strong> <%= new Date(expense.createdAt).toLocaleString() %></p>
    <p><strong>Updated At:</strong> <%= new Date(expense.updatedAt).toLocaleString() %></p>
    
    <% if (expense.imageUrl) { %>
        <img src="<%= expense.imageUrl %>" width="200" alt="Expense Image" />
    <% } %>
    <br>
    <a href="/">Back to list</a>
</body>
</html>
